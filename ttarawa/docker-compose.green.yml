version: '3.8'

services:
  app:
    image: app:0.2
    container_name: app_green
    build:
      dockerfile: Dockerfile
    environment:
      - "spring_profiles_active=green"
    ports:
      - "8086:8080"